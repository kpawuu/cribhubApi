import type { HookContext } from '@feathersjs/feathers';
/**
 * Sanitize users. After-hook for '/users' service.
 */
export declare function removeVerification<H extends HookContext = HookContext>(ifReturnTokens?: boolean): (context: H) => H;
